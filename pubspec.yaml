name: certbot
description: Busslina's Let's Encrypt & Cerbot package.
version: 1.0.0
repository: https://github.com/busslina/certbot

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
